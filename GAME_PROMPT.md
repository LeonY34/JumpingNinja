# GAME_PROMPT.md

此文档用于说明开发者对游戏的设计要求。

## v1玩法介绍

- 用户的操作：1. 点左半边屏幕，ninja的运动速度会改变为左上方（偏离竖直线theta=15度，theta角度应当让开发者通过inspector可调）；2. 点击右半边屏幕，ninja的运动速度会改变为右上方（偏离竖直线theta度，theta角度应当让开发者通过inspector可调）；3. 任意时刻：Ninja会作自由落体运动。
- Ninja是一个方块形的二维实体，初版可以先做成一个没有动画的方块，大小为1*1；
- 地图是一个方格图，每个方格大小都是1*1，有三种类型方块：1. 白色外墙块：只有地图左右两边会有，表示地图的边缘，Ninja碰到后横向速度会瞬间变为0，但是纵向速度不变；2. 黑色方块：Ninja碰到就会死；3. 空方块：Ninja在空方块可以自由移动。
- 视角中心始终是Ninja，除非Ninja太靠地图边缘（视角不会看到地图外的东西）。地图的横向有x=30格（开发者可调，包括最左和最后的两竖排白色外墙），camara显示横向范围x_c=15格（开发者可调），纵向按照手机大小自动计算。
- Ninja一开始会出现在camara最中心（y坐标可调），然后开始下落。一开始在第0层，第0层下面有满的黑块。每一层都是（y_layer=30开发者可调）块，两层交界处保证有一个或者两个（随机）大小为1*3的空格子，其他都是黑格子；
- 前5层没有其他黑格子，5-10层有1个随机位置的黑格子，11-20层有两个随机位置的黑格子，以此类推。保证每个交界处3*3范围内没有黑格子。
- 进入游戏时，先经过加载界面（在加载页面应该有Logo）。加载界面完成后，有排行榜，切换用户，和开始游戏三个选项。如果没有登录过，一开始应当让用户创建用户，让他随便输入名字（只需用户名），然后记录到用户表里面。每个用户应该记录最高分。点击开始游戏后就可以开始游戏，左上角应当显示当前分数以及下一个能超越的用户的分数（包括自己）。如果超过某个用户，或者破纪录，左上角都应该显示几秒。
- 只有无限模式：关卡有无限的层数，每高一层可以升一级。以死掉地方的级数为最终分数。
- 应当有个暂停键，暂停结束后三秒继续。